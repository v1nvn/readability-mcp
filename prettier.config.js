/**
 * @see https://prettier.io/docs/configuration
 * @type {import("prettier").Config}
 */
const config = {
  semi: true,
  trailingComma: 'all',
  singleQuote: true,
  tabWidth: 2,
  printWidth: 80,
  endOfLine: 'auto',
  useTabs: false,
  arrowParens: 'avoid',
};

export default config;
